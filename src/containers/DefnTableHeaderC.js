import { connect } from 'react-redux'

import DefnTableHeader from '../components/DefnTableHeader'

const mapStateToProps = (state, ownProps) => ({
})

const mapDispatchToProps = (dispatch, ownProps) => ({
})

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(DefnTableHeader)
